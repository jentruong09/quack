<div class="posts list-group">
    {{#each posts}}
        <a class="list-item" href="/profile/edit/{{id}}">
            <span>{{title}}</span>
            <span>{{format_date createdAt}}</span>
        </a>
    {{/each}}
</div>

