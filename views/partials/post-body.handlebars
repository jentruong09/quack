<div class="container">
    <div class="card">
        <div class="post-body card-body">
            <div class="card-text">
                <a href="/post/{{id}}">{{post}}</a>
            </div>
            <br>
            <div class="info card-subtitle text-muted">
                By <a href="/api/user/{{id}}">{{user.username}}</a> Posted on {{format_date created_at}}
            </div>
        </div>
    </div>
</div>